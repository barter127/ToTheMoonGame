#pragma once

// Player statistics.
static double money = 1000; // Needs negative values for possible debt mechanic.
static unsigned int assetOwned = 0;
static float assetPrice = 25.0;
static unsigned int day = 1; // Maybe problematic for long play sessions.